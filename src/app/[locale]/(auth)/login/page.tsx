import AuthForm from "@/components/authComponents/AuthForm";
import React from "react";

const Login = () => {
    return (
        <div>
            <AuthForm mode="login" />
        </div>
    );
};

export default Login;
